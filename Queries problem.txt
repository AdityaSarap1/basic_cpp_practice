//Queries problem
#include<iostream>
#include<vector>
using namespace std;
int main()
{
    int n;
    cin>>n
    vector<int>vect(n);
    for(int i=1; i<=n; i++)
    {
        cin>>vect[i];
    }
    const int N = 1e5;
    vector<int>freq(N, 0);
    for(i=0; i<n; i++)
    {
        freq[v[i]]++
    }
    
    int q;
    cout<<"Enter number of queries";
    cin>>q; 
    
    while(q--)
    {
        int freqEle;
        cin>>freqEle;
        cout<<freq[freqEle];
    }
    
 
    
    return 0;
}